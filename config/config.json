{
"specs":[
    {
        "name":"Array match example",
        "type":"array",
        "url":"https://d.ddd.dd.netdprojects/{*}",
        "arrayOrPattern":["abi","krishna"],
        "func":"{if(arrayOrPattern.findIndex(function(element){return clipboard==element})!=-1){return url.replace('{*}',clipboard);}else{return false;}}"
    },
    {
        "name":"Pattern Match Example",
        "type":"pattern",
        "url":"{*}",
        "arrayOrPattern":"www",
        "func":"{let re = new RegExp(arrayOrPattern);if(re.test(clipboard)){return 'http://'+url.replace('{*}',clipboard);}else{return false;}}"
    },   
    {
        "name":"if URL",
        "type":"pattern",
        "url":"{*}",
        "arrayOrPattern":"www",
        "func":"{let re = new RegExp(arrayOrPattern);if(re.test(clipboard)){return 'http://'+url.replace('{*}',clipboard);}else{return false;}}"
    }  
    ]
}